# AI Agent - Помощник продавца на Avito

Интеллектуальный агент для автоматизации общения продавца с покупателями на платформе Avito. Использует LangChain/LangGraph для управления диалогами и LangFuse для observability.

## Возможности

- **Естественный диалог** с контекстом и slot filling
- **Ответы о товарах** через RAG (характеристики, гарантии, качество)
- **Проверка наличия** товара
- **Валидация доставки** (соответствие требованиям СДЭК, Яндекс.Доставка, etc.)
- **Торг по правилам** с встречными предложениями
- **Планирование встреч** с интеграцией календаря
- **Защита от галлюцинаций** через guardrails
- **Полная observability** через LangFuse

## Архитектура

Система построена на **LangGraph State Machine** с интеллектуальной маршрутизацией и самопроверкой качества.

### Граф обработки запросов

```
                        User Message
                             ↓
                   ┌─────────────────┐
                   │ Classify Intent │ (DeepSeek LLM)
                   └────────┬────────┘
                            ↓
                   ┌─────────────────┐
                   │  Check Slots    │ (Slot Filling)
                   └────────┬────────┘
                            ↓
                   ┌─────────────────┐
                   │Route by Complexity│
                   └─────┬──────┬────┘
                         │      │
            Сложные ←────┘      └───→ Простые
                ↓                        ↓
        ┌──────────────┐        ┌──────────────────┐
        │   Planning   │───────→│ Intelligent      │
        │   (LLM Plan) │        │ Router (LLM)     │
        └──────────────┘        └─────────┬────────┘
                                          ↓
                ┌─────────────────────────┴────────────────────┐
                │                                               │
    ┌───────────▼──────────┐                    ┌──────────────▼────────┐
    │  RAG Search          │                    │  Action Nodes:        │
    │  (Hybrid Retriever)  │                    │  • Stock Check        │
    │  • Semantic Search   │                    │  • Delivery Check     │
    │  • Keyword Search    │                    │  • Bargaining         │
    │  • Query Expansion   │                    │  • Meeting Planning   │
    │  • LLM Reranking     │                    │    (Google Calendar)  │
    └───────────┬──────────┘                    └──────────────┬────────┘
                │                                               │
                └───────────────────┬───────────────────────────┘
                                    ↓
                        ┌──────────────────┐
                        │ Generate Response│ (DeepSeek LLM)
                        └────────┬─────────┘
                                 ↓
                        ┌──────────────────┐
                        │   Reflection     │ (Quality Check)
                        └─────┬───────┬────┘
                              │       │
                   Недостатки ←┘       └→ Качественно
                              ↓               ↓
                        Regenerate          END
```

### Ключевые компоненты

#### 1. **LangGraph State Machine**
- **classify_intent**: Классификация намерения пользователя (DeepSeek)
- **check_slots**: Извлечение сущностей и slot filling
- **route_by_complexity**: Определение сложности запроса
  - Простые → прямая маршрутизация
  - Сложные → через Planning
- **planning**: Создание многошагового плана для сложных запросов
- **intelligent_router**: LLM-based маршрутизация к нужному action узлу
- **reflection**: Проверка качества ответа и регенерация при необходимости

#### 2. **RAG System (Hybrid Retriever)**
```
Query → Query Expansion (LLM) → Parallel Search:
                                 ├─ Semantic (ChromaDB + MPNet)
                                 └─ Keyword (Product DB)
                                        ↓
                                  Combine Results
                                        ↓
                                  LLM Reranking
                                        ↓
                                  Top-K Results
```
- **Semantic Search**: ChromaDB + `paraphrase-multilingual-mpnet-base-v2`
- **Keyword Search**: BM25-подобный поиск по продуктам
- **Query Expansion**: Расширение запроса через LLM
- **LLM Reranking**: Переранжирование результатов для точности

#### 3. **Action Nodes**
- **RAG Search**: Поиск информации о товарах через Hybrid Retriever
- **Stock Check**: Проверка наличия с поддержкой вариантов (память, цвет)
- **Delivery Check**: Валидация доставки (СДЭК, Яндекс, Почта России)
- **Bargaining**: Торг с negotiation engine и уведомлениями в Telegram
- **Meeting Planning**: Планирование встречи с интеграцией Google Calendar

#### 4. **Интеграции**
- **Google Calendar API**: Создание событий встреч
- **Telegram Bot**: Уведомления о сделках и встречах
- **DeepSeek LLM**: Intent classification, routing, planning, response generation, reflection
- **LangFuse**: Observability и трейсинг всех LLM вызовов

#### 5. **Guardrails**
- **Source Validation**: Проверка relevance score
- **Reflection Loop**: LLM-based проверка качества ответа
- **Structured Outputs**: JSON для классификаций
- **Fact Checking**: Валидация на основе базы знаний
- **Fallback Responses**: Честное "не знаю" при отсутствии данных

## Установка

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
# LLM Configuration
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# Google Calendar (заполнится автоматически после настройки)
GOOGLE_CALENDAR_REFRESH_TOKEN=
GOOGLE_CALENDAR_CREDENTIALS_FILE=credentials.json
GOOGLE_CALENDAR_ID=primary

# Токен бота для отправки уведомлений
TELEGRAM_BOT_TOKEN=
# Айди профиля кому отправлять уведомления
TELEGRAM_CHAT_ID=
```

### 3. Настройка Google Calendar API

Для работы планирования встреч необходимо настроить интеграцию с Google Calendar.

#### 3.1. Получение credentials.json

1. **Перейдите в [Google Cloud Console](https://console.cloud.google.com/)**
   
2. **Создайте новый проект** (или выберите существующий):
   - Нажмите на выпадающий список проектов вверху страницы
   - Выберите "New Project" / "Новый проект"
   - Укажите название проекта (например, "AI Agent Calendar")
   - Нажмите "Create" / "Создать"

3. **Включите Google Calendar API**:
   - Перейдите в "APIs & Services" → "Library" / "API и сервисы" → "Библиотека"
   - Найдите "Google Calendar API"
   - Нажмите на него и выберите "Enable" / "Включить"

4. **Создайте OAuth 2.0 credentials**:
   - Перейдите в "APIs & Services" → "Credentials" / "API и сервисы" → "Учетные данные"
   - Нажмите "Create Credentials" / "Создать учетные данные"
   - Выберите "OAuth client ID" / "Идентификатор клиента OAuth"
   - Если требуется, настройте "OAuth consent screen":
     - User Type: External
     - App name: AI Agent Calendar
     - User support email: ваш email 
     - Test users: добавьте ваш Google аккаунт
   - Application type: **Desktop app** / **Настольное приложение**
   - Name: AI Agent Desktop
   - Нажмите "Create" / "Создать"

5. **Скачайте credentials.json**:
   - После создания появится окно с Client ID и Client Secret
   - Нажмите "Download JSON" / "Скачать JSON"
   - Сохраните файл как `credentials.json` в **корень проекта**

#### 3.2. Получение Refresh Token

После получения `credentials.json` запустите FastAPI приложение для авторизации:

```bash
# Запуск веб-сервера для авторизации
uvicorn scripts.google_calendar_auth_app:app --reload --port 8080

# Или альтернативный способ
python scripts/google_calendar_auth_app.py
```

Затем:
1. Откройте в браузере: **http://localhost:8080**
2. Нажмите кнопку "Начать авторизацию"
3. Выберите Google аккаунт и разрешите доступ к календарю
4. Вы будете перенаправлены обратно с успешным сообщением
5. Refresh token автоматически сохранится в `.env`

**Важно**: Сохраните refresh token в безопасном месте. Он не имеет срока действия и позволяет приложению получать доступ к календарю без повторной авторизации.

<details>
<summary>📟 Консольная версия (устаревшая)</summary>

Также доступна консольная версия с базовым HTTP сервером:

```bash
python scripts/google_calendar_auth.py
```

</details>

#### 3.3. Проверка настройки

После настройки проверьте интеграцию:

```python
from src.integrations.calendar_service import get_calendar_service

# Инициализация сервиса
calendar = get_calendar_service()

# Проверка доступности времени
is_free = calendar.check_availability(
    date_str="2025-12-10",
    time_str="14:00",
    duration_minutes=30
)

print(f"Время {'свободно' if is_free else 'занято'}")
```

📖 **Подробная документация**: см. `all/docs/calendar_quick_start.md`

### 4. Настройка мониторинга с Langfuse

```bash
# Запустить Langfuse
docker-compose up -d

# Открыть http://localhost:3000
# Создать аккаунт → проект → скопировать API ключи

# Добавить ключи в .env
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_ENABLED=true
```

**Подробная инструкция**: см. `docs/MONITORING_SETUP.md`

**Что отслеживается автоматически**:
- Все LLM вызовы с input/output
- Token usage и стоимость
- Latency каждого узла
- Качество ответов (scores)
- Структурированные логи

### 5. Инициализация векторной базы данных

```bash
# Загрузить данные о товарах в ChromaDB
python scripts/setup_vectorstore.py
```

## Использование

### Консольный режим (для тестирования)

```bash
# Запуск в интерактивном режиме
python src/main.py --mode console

```

### Примеры диалогов

```
Вы: Здравствуйте! А какой процессор в iPhone 15 Pro?
Агент: Здравствуйте! В iPhone 15 Pro установлен процессор Apple A17 Pro...

Вы: Возьму за 90 тысяч
Агент: Давайте встретимся посередине? Могу предложить 92 000 руб...

Вы: Договорились! Где можем встретиться?
Агент: Отлично! Могу на метро Маяковская, Белорусская или в ТЦ Европейский...
```


## Конфигурация

### Товары (`data/products.json`)

Добавьте или измените товары в формате:

```json
{
  "id": "001",
  "title": "Название товара",
  "price": 10000,
  "min_price": 9000,
  "stock": 5,
  "bargaining_allowed": true,
  "max_discount_percent": 10,
  ...
}
```

### Правила торга (`data/bargaining_rules.yaml`)

Настройте стратегию переговоров:

```yaml
default_strategy:
  allow_bargaining: true
  max_acceptable_discount: 10  # %
  counter_offer_strategy: "meet_halfway"
```

### Правила доставки (`data/delivery_rules.yaml`)

Настройте ограничения служб доставки:

```yaml
cdek:
  max_weight: 30.0  # кг
  max_length: 150.0  # см
  ...
```

### Observability & Мониторинг

**Архитектура мониторинга**:
```
Agent Nodes → Tracers → Langfuse
     ↓          ↓           ↓
  Metrics → Collector → Dashboard
     ↓
 Structured Logs
```

**Автоматически собираются**:
- ⏱️ Latency каждого узла графа
- 🪙 Token usage и стоимость LLM вызовов
- 🎯 Intent classification confidence
- 📊 RAG relevance scores
- ✅ Response quality scores
- 📝 Structured logs с session context

**Dashboard Langfuse**: http://localhost:3000
- Traces: детальный просмотр каждого запроса
- Sessions: группировка по пользователям
- Metrics: токены, латентность, стоимость
- Scores: качество и релевантность

## Защита от галлюцинаций

1. **RAG-based responses** - ответы только на основе базы знаний
2. **Source validation** - проверка relevance score
3. **Structured outputs** - JSON для классификации
4. **Fact checking** - валидация цифр и фактов
5. **Fallback responses** - честное "не знаю" при отсутствии данных

## Разработка

### Добавление новых товаров

1. Обновите `data/products.json`
2. Запустите `python scripts/setup_vectorstore.py`

### Переавторизация Google Calendar

Если токен перестал работать или нужно использовать другой аккаунт:

```bash
# Запустить веб-интерфейс авторизации
uvicorn scripts.google_calendar_auth_app:app --port 8080

# Или консольную версию
python scripts/google_calendar_auth.py
```

### Отладка

Установите `LOG_LEVEL=DEBUG` в `.env` для подробных логов.

## TODO

- [ ] Реальная интеграция с Avito API
- [ ] Резервация товаров в БД
- [ ] Метрики и dashboard
